package com.consensus.consensus.global.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // later: JWT filter, password encoder beans, CORS config
}